
#update: Hibernate changes the database according to the given entity structures.
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/db
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
server.address=0.0.0.0
server.port=8080

#uncomment this to initialize Database from the data.sql file
#when starting the project first time after deleting all data, the following 3 lines have to be commented:
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always
#spring.sql.init.continue-on-error=true

#Dump the queries to standard out and beautifies them
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# tuning the auto-configured MultipartConfigElement image size
spring.servlet.multipart.max-file-size=128KB
spring.servlet.multipart.max-request-size=128KB


